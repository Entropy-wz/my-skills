# Recommended workflows

**Canonical menu (agents + install):**  
[`skills/orchestration/build-loop/workflows/recommended.md`](../../skills/orchestration/build-loop/workflows/recommended.md)

The thin `/build-loop` skill loads the **skill-local** copy so copy-install into
`~/.cursor/skills/build-loop/` still works (same pattern as `research-case-card` templates).

> **Edit policy (ADR-001):** change **only** the skill-local file above.  
> Do not maintain a second full copy here — this stub must stay a pointer to avoid dual-source drift.
