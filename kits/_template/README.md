# Kit: \<kit-name\>

One-line purpose.

## Prerequisites

- …

## Quick start

```powershell
# e.g. docker compose, CLI, etc.
```

## Skill

If `skill/SKILL.md` exists, `scripts/install.*` will install it as `~/.cursor/skills/<kit-name>/`.

## Layout

- `skill/` — Cursor skill
- `tools/` — scripts used by the skill/agent
- `agents/` — small agent prompts / runners
- `docker/` — compose / container config
