# Agents

**Cross-kit small agents** — reusable prompt packs or thin runners that are not tied to a single kit.

Kit-local agents stay under `kits/<name>/agents/`. Use this top-level folder for agents shared across kits (e.g. a generic research runner).
